# 已知问题与待优化项

> 文档编号：VNERP-TECH-011 | 版本：V2.0 | 基于：代码现状 2026-06-30

## 一、数据模型层

### DATA-001 Schema 与运行时 SQL 表名不一致【高优先级】

**现象**：`src/lib/db/schema.ts`（Drizzle 定义）与 API Route 运行时 SQL 使用的表名严重不一致。

| schema.ts 定义 | 运行时 SQL 实际使用 | 说明 |
|---------------|-------------------|------|
| `customers` | `crm_customer` | 缺 `crm_` 前缀 |
| `products` | `inv_material` / `prd_product` | 模块归属混乱 |
| `materials` | `inv_material` | 缺 `inv_` 前缀 |
| `suppliers` | `pur_supplier` | 缺 `pur_` 前缀 |
| `inventoryBatches` | `inv_inventory_batch` | 缺 `inv_` 前缀 |
| `workOrders` | `prod_work_order` | 缺 `prod_` 前缀 |
| `purchaseOrders` | `pur_order` / `pur_purchase_order` | 双重命名冲突 |

**影响**：`drizzle-kit generate` 生成的迁移与生产表结构不匹配，`drizzleDb.query.*` 类型化查询失效。
**建议**：将 schema.ts 全部对齐为运行时表名，重新生成迁移基线。

### DATA-002 actual_schema.sql 实际为空【中优先级】

**现象**：`database/actual_schema.sql` 仅含 `DROP TABLE IF EXISTS v_purchase_request_full;`，并非真实表结构。
**影响**：新环境部署无可用 SQL 脚本，仅能依赖 `drizzle/` 迁移目录（仅 2 个迁移）。
**建议**：从生产库导出完整 DDL，或通过 `drizzle-kit push` 同步。

### DATA-003 财务模块三套表名并存【高优先级】

**现象**：财务相关代码同时引用三套表名：

| 来源 | 应收表名 | 应付表名 | 字段 |
|------|---------|---------|------|
| `src/lib/finance-core.ts` | `fin_receivable` | `fin_payable` | `balance`, `received_amount`, `status`(1/2/3) |
| `src/app/api/finance/aging/route.ts` | `finance_receivable` | `finance_payable` | `remaining_amount`, `status='settled'` |
| `src/lib/soft-delete.ts` 配置 | `fin_receivable` | `fin_payable` | `cancelStatus='cancelled'`(字符串) |

**影响**：账龄分析查询必然失败（表不存在）；软删除 `cancelled` 与数字状态不兼容。
**建议**：统一为 `fin_receivable`/`fin_payable`，状态改为枚举。

## 二、库存与单据层

### INV-001 soft-delete 表名与库存 API 不匹配【高优先级】

**现象**：`src/lib/soft-delete.ts` 的 `CANCELLABLE_DOCUMENTS` 配置使用 `warehouse_inbound`/`warehouse_outbound`/`warehouse_stocktaking`/`warehouse_transfer`，而 `src/app/api/inventory/route.ts` 实际操作 `inv_inventory_batch`。
**影响**：调拨/盘点作废调用 `quickCancel('transfer_order', ...)` 会失败。
**建议**：对齐为 `inv_` 前缀表名，并补齐 `inbound_order`/`outbound_order` 的真实表。

### INV-002 制造费用分摊为硬编码【中优先级】

**现象**：`src/lib/finance-core.ts` 第 346 行 `manufacturingCost = laborCost * 0.5`，注释标明"简化计算"。
**影响**：成本核算结果不准确，无法满足月末结账要求。
**建议**：接入车间制造费用总额表，按工时比例分摊。

## 三、运行时基础设施

### RUN-001 限流为进程内内存【中优先级】

**现象**：`src/lib/rate-limit.ts` 使用内存 `Map` 存储（60s 清理，max 10000 条）。
**影响**：多实例部署时限流失效，单实例上限被绕过。
**建议**：切换为 Redis 计数器（项目已引入 `ioredis`）。

### RUN-002 Token 黑名单多实例不同步【高优先级】

**现象**：`src/lib/token-blacklist.ts` 优先 Redis，但 Redis 不可用时降级为内存 `Set`，且未持久化。
**影响**：实例重启或 Redis 故障期间，已注销 Token 可继续访问。
**建议**：生产环境强制 Redis，禁用降级；Redis 故障应快速失败而非降级。

### RUN-003 事件总线仅内存/DB 两选【低优先级】

**现象**：`EVENT_BUS_TYPE=memory|db`，memory 模式不支持多实例。
**建议**：生产环境强制 `db`（Outbox 模式），参考《系统可审计性设计方案》。

## 四、测试体系

### TEST-001 部分测试因表缺失失败【中优先级】

**现象**：`tests/unit/` 下 9 个测试文件失败，主因是 Mock 未覆盖的表或 schema 不一致（如 `sal_order_detail` 缺 `material_name` 字段、`fin_*` 表名不匹配）。
**影响**：CI 流水线不稳定，覆盖率统计 89.36% 但存在 flaky。
**建议**：统一 Mock 表结构，对齐真实字段名。

### TEST-002 测试环境 window 未定义【低优先级】

**现象**：`tests/unit/setup.ts` 引用 `window` 在 Node 环境报错，已通过 `typeof window !== 'undefined'` 修复。
**建议**：将浏览器全局 Mock 移至 `vitest.setup.ts`。

## 五、已解决（保留记录）

| 编号 | 问题 | 解决方案 |
|------|------|---------|
| ARCH-001 | 两套报工系统并存 | 统一使用 `prd_work_report` |
| ARCH-002 | `qr_codes` 与 `qrcode_record` 不互通 | 统一 `qrcode_record`，加 `split_flag`/`parent_qr_id` |
| ARCH-003 | 冗余盘点/调拨表 | 统一 `inv_stocktaking`/`inv_transfer_order` |
| ARCH-004 | 冗余财务表 | 统一 `fin_receivable`/`fin_payable` |
| ARCH-005 | `standard_cards` 未使用 | 统一 `prd_standard_card` |
| AUTH-001 | Token 无刷新机制 | 已实现 Refresh Token（`src/app/api/auth/refresh`） |

> 最后更新：2026-06-30
