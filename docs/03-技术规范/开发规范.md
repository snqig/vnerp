# VNERP 开发规范

> 文档编号：VNERP-TECH-001  
> 版本：V1.0  
> 更新日期：2026-05-10

---

## 1. 总则

### 1.1 规范目的

- 统一代码风格，提高代码可读性
- 减少低级错误，提高代码质量
- 便于团队协作和代码维护
- 确保系统稳定性和可扩展性

### 1.2 适用范围

本规范适用于 VNERP 项目所有开发人员，包括：

- 前端开发（Next.js + React）
- 后端开发（API Routes）
- 数据库开发（MySQL + Drizzle）
- 脚本开发（Node.js）

---

## 2. 命名规范

### 2.1 文件命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | PascalCase | `UserProfile.tsx` |
| 页面文件 | kebab-case | `user-profile/page.tsx` |
| API 路由 | kebab-case | `user-profile/route.ts` |
| 工具函数 | camelCase | `formatDate.ts` |
| 类型定义 | PascalCase | `UserTypes.ts` |
| 样式文件 | kebab-case | `user-profile.css` |

### 2.2 变量命名

```typescript
// ✅ 正确
const userName = 'John';
const userList = [];
const isLoading = true;
const hasPermission = false;

// ❌ 错误
const UserName = 'John';
const user_list = [];
const loading = true;
```

### 2.3 函数命名

```typescript
// ✅ 正确 - 动词开头，表达清晰
function getUserById(id: number) {}
function calculateTotal(items: Item[]) {}
function validateEmail(email: string) {}
async function fetchUserData() {}

// ❌ 错误
function user() {}
function data() {}
function get() {}
```

### 2.4 常量命名

```typescript
// ✅ 正确 - 全大写，下划线分隔
const MAX_RETRY_COUNT = 3;
const API_BASE_URL = 'https://api.example.com';
const DEFAULT_PAGE_SIZE = 20;

// ❌ 错误
const maxRetryCount = 3;
const apiBaseUrl = 'https://api.example.com';
```

### 2.5 数据库命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 表名 | snake_case，复数 | `users`, `work_orders` |
| 字段名 | snake_case | `user_name`, `create_time` |
| 索引名 | idx_表名_字段 | `idx_users_email` |
| 外键名 | fk_表名_关联表 | `fk_orders_users` |

---

## 3. 代码规范

### 3.1 TypeScript 规范

#### 3.1.1 类型定义

```typescript
// ✅ 正确 - 使用 interface 定义对象类型
interface User {
  id: number;
  name: string;
  email: string;
  role: 'admin' | 'user' | 'guest';
}

// ✅ 正确 - 使用 type 定义联合类型、工具类型
type Status = 'pending' | 'approved' | 'rejected';
type UserResponse = ApiResponse<User>;

// ❌ 错误 - 使用 any
function processData(data: any) {}
```

#### 3.1.2 函数定义

```typescript
// ✅ 正确 - 明确参数和返回类型
async function getUserById(id: number): Promise<User | null> {
  const user = await db.select().from(users).where(eq(users.id, id));
  return user[0] || null;
}

// ✅ 正确 - 使用默认参数
function formatDate(date: Date, format: string = 'YYYY-MM-DD'): string {
  // ...
}

// ❌ 错误 - 缺少返回类型
async function getUserById(id: number) {
  // ...
}
```

#### 3.1.3 错误处理

```typescript
// ✅ 正确 - 使用 try-catch 处理异步错误
async function fetchData() {
  try {
    const response = await fetch('/api/data');
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    return await response.json();
  } catch (error) {
    console.error('Failed to fetch data:', error);
    throw error;
  }
}

// ✅ 正确 - 使用自定义错误类型
class ValidationError extends Error {
  constructor(message: string, public field: string) {
    super(message);
    this.name = 'ValidationError';
  }
}
```

### 3.2 React 组件规范

#### 3.2.1 组件结构

```typescript
// ✅ 正确 - 组件结构清晰
interface UserCardProps {
  user: User;
  onEdit?: (id: number) => void;
}

export function UserCard({ user, onEdit }: UserCardProps) {
  // 1. Hooks
  const [isExpanded, setIsExpanded] = useState(false);
  
  // 2. 派生状态
  const displayName = user.name || 'Unknown';
  
  // 3. 事件处理
  const handleEdit = () => {
    onEdit?.(user.id);
  };
  
  // 4. 渲染
  return (
    <div className="user-card">
      <h3>{displayName}</h3>
      <button onClick={handleEdit}>编辑</button>
    </div>
  );
}
```

#### 3.2.2 条件渲染

```typescript
// ✅ 正确 - 使用 && 和 三元运算符
{isLoading && <Spinner />}
{user ? <UserProfile user={user} /> : <EmptyState />}
{items.length > 0 ? (
  <ItemList items={items} />
) : (
  <EmptyMessage />
)}

// ❌ 错误 - 嵌套过深
{isLoading ? (
  isFetching ? <Spinner /> : <Loading />
) : (
  user ? <UserProfile /> : <Empty />
)}
```

### 3.3 API 路由规范

#### 3.3.1 路由结构

```typescript
// ✅ 正确 - 标准路由结构
import { NextRequest, NextResponse } from 'next/server';
import { z } from 'zod';

// 请求验证 Schema
const createSchema = z.object({
  name: z.string().min(1),
  email: z.string().email(),
});

// GET 处理
export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const page = parseInt(searchParams.get('page') || '1');
    const pageSize = parseInt(searchParams.get('pageSize') || '20');
    
    // 业务逻辑
    const result = await getData({ page, pageSize });
    
    return NextResponse.json({
      code: 200,
      message: 'success',
      data: result,
    });
  } catch (error) {
    console.error('GET error:', error);
    return NextResponse.json(
      { code: 500, message: 'Internal Server Error' },
      { status: 500 }
    );
  }
}

// POST 处理
export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const validated = createSchema.parse(body);
    
    // 业务逻辑
    const result = await createData(validated);
    
    return NextResponse.json({
      code: 200,
      message: 'success',
      data: result,
    });
  } catch (error) {
    if (error instanceof z.ZodError) {
      return NextResponse.json(
        { code: 400, message: 'Validation Error', errors: error.errors },
        { status: 400 }
      );
    }
    console.error('POST error:', error);
    return NextResponse.json(
      { code: 500, message: 'Internal Server Error' },
      { status: 500 }
    );
  }
}
```

#### 3.3.2 响应格式

```typescript
// ✅ 正确 - 统一响应格式
interface ApiResponse<T> {
  code: number;
  message: string;
  data?: T;
  errors?: Array<{ field: string; message: string }>;
}

// 成功响应
{
  "code": 200,
  "message": "success",
  "data": { ... }
}

// 分页响应
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [...],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}

// 错误响应
{
  "code": 400,
  "message": "Validation Error",
  "errors": [
    { "field": "name", "message": "名称不能为空" }
  ]
}
```

---

## 4. 样式规范

### 4.1 CSS 变量使用

```css
/* ✅ 正确 - 使用 CSS 变量 */
.card {
  background: var(--bg-card);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
}

.button {
  background: var(--primary-color);
  color: var(--text-white);
}

/* ❌ 错误 - 硬编码颜色 */
.card {
  background: #ffffff;
  color: #333333;
  border: 1px solid #e0e0e0;
}
```

### 4.2 Tailwind CSS 规范

```typescript
// ✅ 正确 - 使用 Tailwind 类名
<div className="flex items-center justify-between p-4 bg-white dark:bg-gray-800 rounded-lg shadow">
  <span className="text-gray-900 dark:text-white">标题</span>
  <button className="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
    按钮
  </button>
</div>

// ✅ 正确 - 提取重复的类名
const buttonStyles = 'px-4 py-2 rounded font-medium transition-colors';
const primaryStyles = `${buttonStyles} bg-blue-500 text-white hover:bg-blue-600`;
const secondaryStyles = `${buttonStyles} bg-gray-200 text-gray-800 hover:bg-gray-300`;
```

### 4.3 颜色变量对照表

| 变量名 | 用途 | 浅色模式 | 深色模式 |
|--------|------|----------|----------|
| `--bg-white` | 白色背景 | #ffffff | #1f2937 |
| `--bg-card` | 卡片背景 | #ffffff | #374151 |
| `--bg-secondary` | 次要背景 | #f8f9fa | #4b5563 |
| `--text-primary` | 主要文字 | #1f2937 | #f9fafb |
| `--text-secondary` | 次要文字 | #6b7280 | #9ca3af |
| `--border-color` | 边框颜色 | #e5e7eb | #4b5563 |
| `--primary-color` | 主色调 | #3b82f6 | #60a5fa |
| `--success-color` | 成功状态 | #10b981 | #34d399 |
| `--warning-color` | 警告状态 | #f59e0b | #fbbf24 |
| `--danger-color` | 错误状态 | #ef4444 | #f87171 |

---

## 5. 数据库规范

### 5.1 表设计规范

```sql
-- ✅ 正确 - 标准表结构
CREATE TABLE users (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) NOT NULL COMMENT '用户名',
  email VARCHAR(100) NOT NULL COMMENT '邮箱',
  password VARCHAR(255) NOT NULL COMMENT '密码',
  status TINYINT DEFAULT 1 COMMENT '状态：0=禁用，1=启用',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  deleted TINYINT DEFAULT 0 COMMENT '软删除：0=未删除，1=已删除',
  
  UNIQUE KEY uk_email (email),
  INDEX idx_username (username),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 5.2 索引规范

| 索引类型 | 命名规则 | 示例 |
|----------|----------|------|
| 主键 | PRIMARY | `PRIMARY KEY (id)` |
| 唯一索引 | uk_字段名 | `UNIQUE KEY uk_email (email)` |
| 普通索引 | idx_字段名 | `INDEX idx_status (status)` |
| 组合索引 | idx_字段1_字段2 | `INDEX idx_status_create_time (status, create_time)` |
| 外键 | fk_表名_关联表 | `FOREIGN KEY fk_orders_users (user_id) REFERENCES users(id)` |

### 5.3 查询规范

```typescript
// ✅ 正确 - 使用参数化查询
const user = await db
  .select()
  .from(users)
  .where(eq(users.email, email));

// ✅ 正确 - 使用事务
await db.transaction(async (tx) => {
  await tx.insert(orders).values(orderData);
  await tx.insert(orderItems).values(itemsData);
});

// ❌ 错误 - SQL 拼接
const query = `SELECT * FROM users WHERE email = '${email}'`;
```

---

## 6. Git 规范

### 6.1 分支命名

| 分支类型 | 命名规则 | 示例 |
|----------|----------|------|
| 主分支 | main | `main` |
| 开发分支 | develop | `develop` |
| 功能分支 | feature/功能名 | `feature/user-auth` |
| 修复分支 | fix/问题描述 | `fix/login-error` |
| 发布分支 | release/版本号 | `release/v1.0.0` |
| 热修复分支 | hotfix/问题描述 | `hotfix/security-patch` |

### 6.2 提交信息

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型：**
- `feat`: 新功能
- `fix`: 修复 Bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例：**
```
feat(auth): 添加用户登录功能

- 实现 JWT 认证
- 添加登录页面
- 添加权限校验中间件

Closes #123
```

---

## 7. 安全规范

### 7.1 敏感信息处理

```typescript
// ✅ 正确 - 使用环境变量
const dbPassword = process.env.DB_PASSWORD;
const jwtSecret = process.env.JWT_SECRET;

// ❌ 错误 - 硬编码敏感信息
const dbPassword = 'password123';
const jwtSecret = 'secret';
```

### 7.2 输入验证

```typescript
// ✅ 正确 - 使用 Zod 验证
const schema = z.object({
  email: z.string().email('邮箱格式不正确'),
  password: z.string().min(8, '密码至少8位'),
  age: z.number().min(0).max(150).optional(),
});

const validated = schema.parse(input);
```

### 7.3 SQL 注入防护

```typescript
// ✅ 正确 - 使用 ORM 参数化查询
const user = await db
  .select()
  .from(users)
  .where(eq(users.id, userId));

// ❌ 错误 - 直接拼接 SQL
const query = `SELECT * FROM users WHERE id = ${userId}`;
```

---

## 8. 性能规范

### 8.1 数据库查询优化

```typescript
// ✅ 正确 - 只查询需要的字段
const users = await db
  .select({ id: users.id, name: users.name })
  .from(users);

// ✅ 正确 - 使用分页
const { page, pageSize } = params;
const offset = (page - 1) * pageSize;
const list = await db
  .select()
  .from(users)
  .limit(pageSize)
  .offset(offset);

// ❌ 错误 - 查询全部再分页
const allUsers = await db.select().from(users);
const list = allUsers.slice(offset, offset + pageSize);
```

### 8.2 前端性能优化

```typescript
// ✅ 正确 - 使用动态导入
const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <Spinner />,
  ssr: false,
});

// ✅ 正确 - 使用 useMemo 缓存计算结果
const sortedItems = useMemo(() => {
  return items.sort((a, b) => a.name.localeCompare(b.name));
}, [items]);

// ✅ 正确 - 使用 useCallback 缓存回调
const handleClick = useCallback((id: number) => {
  // ...
}, [dependency]);
```

---

## 9. 测试规范

### 9.1 单元测试

```typescript
// ✅ 正确 - 测试用例结构
describe('formatDate', () => {
  it('should format date correctly', () => {
    const date = new Date('2026-05-10');
    const result = formatDate(date, 'YYYY-MM-DD');
    expect(result).toBe('2026-05-10');
  });

  it('should handle invalid date', () => {
    const result = formatDate(null as any, 'YYYY-MM-DD');
    expect(result).toBe('');
  });
});
```

### 9.2 测试覆盖率要求

| 类型 | 最低覆盖率 |
|------|------------|
| 核心业务逻辑 | 80% |
| API 路由 | 70% |
| 工具函数 | 90% |
| 组件 | 60% |

---

## 10. 文档规范

### 10.1 代码注释

```typescript
/**
 * 根据ID获取用户信息
 * @param id 用户ID
 * @returns 用户信息，如果不存在返回null
 * @throws {NotFoundError} 用户不存在时抛出
 */
async function getUserById(id: number): Promise<User | null> {
  // ...
}

// 单行注释 - 解释复杂逻辑
const discount = price * 0.9; // VIP用户享受9折优惠
```

### 10.2 API 文档

```typescript
/**
 * @api {get} /api/users 获取用户列表
 * @apiName GetUsers
 * @apiGroup User
 * 
 * @apiParam {Number} [page=1] 页码
 * @apiParam {Number} [pageSize=20] 每页数量
 * @apiParam {String} [keyword] 搜索关键词
 * 
 * @apiSuccess {Object} data 响应数据
 * @apiSuccess {Array} data.list 用户列表
 * @apiSuccess {Number} data.total 总数
 * 
 * @apiError (400) BadRequest 参数错误
 * @apiError (500) InternalServerError 服务器错误
 */
```

---

## 11. 检查清单

### 11.1 代码提交前检查

- [ ] 代码符合命名规范
- [ ] 没有硬编码颜色
- [ ] 没有硬编码敏感信息
- [ ] 类型定义完整
- [ ] 错误处理完善
- [ ] 必要的注释已添加
- [ ] 测试用例已通过
- [ ] 提交信息符合规范

### 11.2 代码审查检查

- [ ] 逻辑正确性
- [ ] 性能优化
- [ ] 安全性检查
- [ ] 代码可读性
- [ ] 是否有更好的实现方式
