# 测试用例模板

> 提供单元测试、E2E 测试、验收用例三类标准模板，复制即用。所有路径以仓库实际目录为准。

## 一、单元测试模板（Vitest）

> 文件位置：`tests/unit/**/*.test.ts`；`include` 规则为 `tests/**/*.test.ts`。

```typescript
/**
 * 模块：BOM 展开
 * 文件：tests/unit/bom-expansion.test.ts
 */
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { expandBom } from '@/lib/bom-expansion';

// 被测对象依赖时通过 vi.mock 替换
vi.mock('@/lib/db', () => ({
  query: vi.fn(),
  execute: vi.fn(),
  transaction: vi.fn((fn) => fn({})),
}));

describe('BOM 展开计算', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('应该正确展开单层 BOM 并加上损耗率', async () => {
    // Arrange
    const productId = 1;
    const quantity = 10;

    // Act
    const result = await expandBom(productId, quantity);

    // Assert
    expect(result.items).toHaveLength(2);
    expect(result.items[0].actualQuantity).toBeCloseTo(21); // 20 * (1 + 5/100)
  });

  it('应该检测循环引用并发出警告', async () => {
    const result = await expandBom(1, 10);
    expect(result.circularReferenceWarnings).toBeDefined();
  });
});
```

### 模板要素

| 字段 | 说明 |
|------|------|
| 模块 | 被测模块名 |
| 文件 | 测试文件相对路径（统一放 `tests/unit/`） |
| Arrange | 准备数据与 Mock |
| Act | 调用被测函数 |
| Assert | 明确断言，避免 `toBeTruthy()` |

### 领域聚合根测试模板

```typescript
/**
 * 模块：仓库·出库单聚合根
 * 文件：tests/unit/domain/warehouse/aggregates/outbound-order.test.ts
 */
import { describe, it, expect } from 'vitest';
import { OutboundOrder } from '@/domain/warehouse/aggregates/outbound-order';

describe('OutboundOrder 聚合根', () => {
  it('草稿状态的出库单可以提交审批', () => {
    const order = new OutboundOrder({ id: 1, status: 'draft', items: [] });
    order.submit();
    expect(order.status).toBe('pending');
  });

  it('已完成状态的出库单不能再次审批', () => {
    const order = new OutboundOrder({ id: 1, status: 'completed', items: [] });
    expect(() => order.submit()).toThrow();
  });
});
```

## 二、E2E 测试模板（Playwright）

> 文件位置：`tests/*.spec.ts`；`testMatch: '**/*.spec.ts'`。

```typescript
/**
 * 模块：登录流程
 * 文件：tests/login.spec.ts
 */
import { test, expect } from '@playwright/test';

test.describe('登录流程', () => {
  test('使用合法凭据登录后跳转看板', async ({ page }) => {
    // 默认 locale zh-CN 不带前缀（localePrefix: 'as-needed'）
    await page.goto('/login');

    await page.fill('input[name="username"]', 'admin');
    await page.fill('input[name="password"]', 'admin123');
    await page.click('button[type="submit"]');

    await expect(page).toHaveURL(/\/dashboard/);
  });

  test('密码错误时显示错误提示', async ({ page }) => {
    await page.goto('/login');
    await page.fill('input[name="username"]', 'admin');
    await page.fill('input[name="password"]', 'wrong');
    await page.click('button[type="submit"]');

    await expect(page.locator('[role="alert"]')).toBeVisible();
  });
});
```

### E2E 模板要素

| 字段 | 说明 |
|------|------|
| `test.describe` | 用模块名分组 |
| `baseURL` | 已在 `playwright.config.ts` 配置为 `http://localhost:5000`，`goto` 用相对路径 |
| 断言 | 优先使用 `toHaveURL`、`toContainText`、`toBeVisible` |
| 清理 | 测试间状态由 `tests/global-setup.ts` 统一重置 |
| 移动端 | 用 `projects` 中预置的 `Mobile Chrome` / `Mobile Safari`，通过 `test.use({ ...devices['Pixel 5'] })` 切换 |

## 三、功能验收用例模板（手工 / UAT）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-{模块}-{序号}，例如 TC-WH-001 |
| **用例名称** | 简短描述被测功能 |
| **测试目的** | 验证点（如：FIFO 批次推荐正确） |
| **优先级** | P0 / P1 / P2 |
| **前置条件** | 1. 已登录<br>2. 数据准备 |
| **测试步骤** | 1. 进入页面<br>2. 操作步骤<br>3. 验证点 |
| **预期结果** | 1. 系统响应<br>2. 数据状态 |
| **实际结果** | |
| **测试状态** | □ 通过 □ 失败 □ 阻塞 |
| **执行人 / 日期** | |
| **备注** | |

## 四、示例：FIFO 批次推荐

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-WH-005 |
| **用例名称** | 系统按入库时间升序推荐批次 |
| **测试目的** | 验证 FIFO 推荐算法（`src/lib/fifo-allocation.ts`） |
| **优先级** | P0 |
| **前置条件** | 存在 3 批同种物料：批次 A（5/1，50）、批次 B（5/5，30）、批次 C（5/10，20）；FIFO 已开启 |
| **测试步骤** | 1. 创建领料单 MR002，需求 60<br>2. 系统自动推荐批次<br>3. 检查推荐顺序与数量 |
| **预期结果** | 1. 推荐顺序：A → B → C<br>2. 分配：A(50) + B(10) = 60<br>3. 不允许手动选其他批次（除非例外审批） |
| **测试状态** | □ 通过 □ 失败 □ 阻塞 |

## 五、测试执行记录模板

### 5.1 执行汇总

| 模块 | 用例总数 | 通过 | 失败 | 阻塞 | 通过率 |
|------|----------|------|------|------|--------|
| 仓库管理 | | | | | |
| 生产管理 | | | | | |
| 财务管理 | | | | | |
| **合计** | | | | | |

### 5.2 缺陷记录

| 缺陷编号 | 模块 | 描述 | 严重程度 | 状态 | 负责人 |
|----------|------|------|----------|------|--------|
| | | | | | |

## 六、相关文档

- [测试指南.md](./测试指南.md)
- [全链路测试方案.md](./全链路测试方案.md)
- [测试工具说明.md](./测试工具说明.md)
- [00-测试文档总览.md](./00-测试文档总览.md)

> 最后更新：2026-07-10
