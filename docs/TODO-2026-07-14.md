# Print MIS TODO 清单

> 生成日期：2026-07-14
> 基于项目全面分析

---

## P0 — 必须修复

### 1. 统一数据库 Schema（Drizzle-first）

**问题**：SQL 文件 163 张表 vs Drizzle ORM 66 张表，97 张表无 ORM 映射；6 个名称冲突。

- [ ] 修复 6 个名称冲突（prd_pick_order vs prd_material_issue 等）
- [ ] 重新启用 drizzle-kit 迁移路径
- [ ] 从数据库导出最新 SQL dump 覆盖 `vnerpdacahng_schema.sql`
- [ ] 运行 `drizzle-kit generate` 生成初始迁移
- [ ] 更新 `setup:db` 和 `migrate` 脚本使用 drizzle-kit
- [ ] 更新 README 命令表（移除已废弃的 db:generate/db:push）

### 2. 修复 Demo 模式完整性

**问题**：`transaction()` 和 `drizzleDb` 不拦截 demo 模式，事务操作会尝试连接真实 MySQL。

- [ ] `transaction()` / `transactionWithRetry()` 添加 demo 模式拦截
- [ ] `drizzleDb` 添加 demo 模式拦截
- [ ] 验证 demo 模式下所有 API 路由正常工作

### 3. 修复 Schema.ts 头信息

**问题**：文件头写"35 张表"，实际是 66 张。

- [ ] `src/lib/db/schema.ts:8` 更新为 66

---

## P1 — 高优先级

### 4. 安全加固

- [ ] 确认 `.env` 中的生产凭据已轮换（DB_PASSWORD, JWT_SECRET）
- [ ] 生产环境必须强制设置 JWT_SECRET，阻止回退到公开的 demo key
- [ ] Demo 模式在生产环境不应可启用（加 NODE_ENV 检查）

### 5. 清理垃圾日志

**问题**：14 处不必要的 `console.log` 散落在源码中。

- [ ] `src/app/[locale]/sample/standard-card/input-card/page.tsx` — 删除 13 行调试日志
- [ ] `src/app/[locale]/sample/standard-card/print/page.tsx:145` — 删除
- [ ] `src/app/[locale]/warehouse/inbound/components/InboundStatsCards.tsx:45` — 删除
- [ ] `src/lib/demo-data.ts:100` — 改用 logger 或加环境开关

### 6. 修复文档不一致

- [ ] README License badge 改为 MIT（当前错误显示 Internal）
- [ ] 统一 Node.js 版本要求（README 说 18+，CONTRIBUTING 说 20+）
- [ ] CONTRIBUTING.md 中 `pnpm setup-db` → `pnpm setup:db`

---

## P2 — 中优先级

### 7. 测试加固

- [ ] 修复 `tests/unit/mrp-engine.test.ts` 中 `vi.mock()` 不在顶层的警告
- [ ] 持续提升覆盖率（当前 1262 测试，目标 80%+）
- [ ] 添加 Demo 模式专用测试（验证所有 mock 路由）

### 8. 降低 ESLint 警告

**问题**：5348 个警告（0 错误），大部分是 `@typescript-eslint/no-explicit-any`。

- [ ] 清理 `no-explicit-any` 警告（逐步，高风险）
- [ ] 修复 `i18n/no-chinese-hardcode` 的 6 处残留（`src/lib/soft-delete.ts`）
- [ ] 清理 `no-unused-vars` 警告

### 9. Schema 逐步对齐

**问题**：97 张表只有 SQL 没有 Drizzle ORM 映射。

- [ ] 为核心业务表（sys_*, crm_*, qc_*, fin_*）添加 Drizzle 映射
- [ ] 逐步为仓库辅助表（inv_*）添加映射
- [ ] 将 raw SQL 查询迁移到 Drizzle ORM

---

## P3 — 长期方向

### 10. 社区建设

- [ ] 创建 `SECURITY.md`（安全策略）
- [ ] 创建 `CODE_OF_CONDUCT.md`（行为准则）
- [ ] 完善 Issue 模板和 PR 模板
- [ ] 在技术社区推广项目

### 11. 性能与稳定性

- [ ] 建立性能基准测试
- [ ] 添加大数据量压力测试
- [ ] 完善错误处理和异常恢复
- [ ] 增加审计日志和操作追溯

### 12. 核心模块深度完善

- [ ] 聚焦 2-3 个核心模块（印前管理 + 生产管理 + 仓储管理）做到深度可用
- [ ] 避免 12 个模块都停留在表面

---

## 当前阻塞项

- [ ] **Vercel 构建成功后**设置 GitHub Website URL 为 `https://vnerp.vercel.app/`
