# Print MIS TODO 清单

> 生成日期：2026-07-14
> 最后更新：2026-07-14
> 基于项目全面分析

---

## ✅ 已完成（本轮修复合集）

### P0
- [x] 财务表名统一（`finance_receivable` → `fin_receivable`，8 个文件）
- [x] Schema 6 个名称冲突修复（`prd_pick_order`→`prd_material_issue` 等）
- [x] `env.ts` 永不模块级抛出（Vercel Turbopack 构建修复）
- [x] Demo 模式完整性（`transaction()`/`drizzleDb` 拦截）
- [x] Schema.ts 头信息（35→66）

### P1
- [x] 安全加固（`isDemoMode()` 生产非 Vercel 阻断、登录路由检查）
- [x] 清理 16 处 debug console.log
- [x] 文档修复（README License 徽章、Node.js 版本、废弃命令；CONTRIBUTING 拼写）
- [x] `warehouse_stock` → `inv_inventory`（INV-001 监控路由）

### P2
- [x] DDD 违规导入修复（2 处 domain→infrastructure）
- [x] `db:generate`/`db:push`/`db:migrate` 脚本恢复（drizzle-kit）
- [x] `actual_schema.sql` 清理指向权威 schema

### INV-002 评估
- 制造费用分摊 `cost.manufacturing_cost_ratio` 已由迁移脚本 `038_create_sys_calc_param.sql` 预置种子数据
- `CalcParamService.getDecimal` 优先读取数据库配置，`0.5` 为代码级兜底
- **结论：无需改代码**

---

## P0 — 必须修复（剩余）

### 1. Token 黑名单持久化

**现状**：`token-blacklist.ts` 已使用 `CacheManager` 抽象层（`getCacheManager()`），配置 `REDIS_URL` 即可自动用 `RedisCacheManager`。代码层面无需修改。

- [ ] 生产环境设置 `REDIS_URL` 环境变量
- [ ] 验证 Redis 故障时仍可降级到 `InMemoryCacheManager`

---

## P1 — 高优先级（剩余）

### 2. 限流 Redis 化

**问题**：`src/lib/rate-limit.ts:83` 当 Redis 不可用时降级到内存 `Map`，多实例部署失效。

- [ ] 生产环境设置 `REDIS_URL`
- [ ] 考虑是否禁止内存降级（根据部署策略决定）

---

## P2 — 中优先级

### 3. 测试加固
- [ ] 修复 `tests/unit/mrp-engine.test.ts` 中 `vi.mock()` 不在顶层
- [ ] 持续提升覆盖率（当前 1262 测试，目标 80%+）
- [ ] 添加 Demo 模式专用测试

### 4. 降低 ESLint 警告
- [ ] 清理 `no-explicit-any` 警告（5348 个，逐步）
- [ ] 修复 `i18n/no-chinese-hardcode` 6 处（`src/lib/soft-delete.ts`）
- [ ] 清理 `no-unused-vars` 警告

### 5. Schema 逐步对齐（97 张缺失表）
- [ ] 为核心业务表（sys_*, crm_*, qc_*, fin_*）添加 Drizzle 映射
- [ ] 逐步为仓库辅助表（inv_*）添加映射
- [ ] 将 raw SQL 查询迁移到 Drizzle ORM

---

## P3 — 长期

### 6. 设备/HR 模块迁移 DDD
- [ ] 评估工作量（约 4-6 天）
- [ ] 创建领域实体、仓储接口
- [ ] 从胖路由迁移到应用服务层

### 7. 社区建设
- [ ] 创建 `SECURITY.md` / `CODE_OF_CONDUCT.md`
- [ ] 完善 Issue/PR 模板

### 8. 性能与稳定性
- [ ] 建立性能基准测试
- [ ] 完善错误处理和审计日志

---

## 当前阻塞项
- [ ] **等待 Vercel 构建通过**（确认 `env.ts` 修复有效）
- [ ] **Vercel 构建通过后**设置 GitHub Website URL 为 `https://vnerp.vercel.app/`
