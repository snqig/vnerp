# Print MIS TODO 清单

> 生成日期：2026-07-14
> 最后更新：2026-07-15
> 基于项目全面分析与 v0.3.0 迭代成果

---

## ✅ 已完成（v0.3.0 迭代）

### 印前/打样/生产模块完善（23/25 任务）
- [x] T001-T003：前置公共准备（BaseRepository、扩展表 Drizzle 定义、领域事件复用）
- [x] T101-T105：印前模块完善（修复 Tool.ts bug、验证现有实现）
- [x] T201-T206：生产模块完善（修复完工入库事件断链）
- [x] T301-T305：打样模块完善（实现一键转大货订单）
- [x] T401-T403：跨模块联动校验（新增 4 个测试文件 24 用例）
- [x] T501：全量回归测试（1262 tests / 68 files 通过，0 ts-check 错误）

### P0 修复
- [x] 财务表名统一（`finance_receivable` → `fin_receivable`，8 个文件）
- [x] Schema 6 个名称冲突修复（`prd_pick_order`→`prd_material_issue` 等）
- [x] `env.ts` 永不模块级抛出（Vercel Turbopack 构建修复）
- [x] Demo 模式完整性（`transaction()`/`drizzleDb` 拦截）
- [x] Schema.ts 头信息（35→66）

### P1 修复
- [x] 安全加固（`isDemoMode()` 生产非 Vercel 阻断、登录路由检查）
- [x] 清理 16 处 debug console.log
- [x] 文档修复（README License 徽章、Node.js 版本、废弃命令；CONTRIBUTING 拼写）
- [x] `warehouse_stock` → `inv_inventory`（INV-001 监控路由）

### P2 修复
- [x] DDD 违规导入修复（2 处 domain→infrastructure）
- [x] `db:generate`/`db:push`/`db:migrate` 脚本恢复（drizzle-kit）
- [x] `actual_schema.sql` 清理指向权威 schema

---

## P0 — 必须修复（发布前阻断项）

### 1. FinishOrderInventoryHandler TOCTOU 竞态修复

**问题**：幂等检查采用"先 SELECT 后 INSERT"模式，存在 TOCTOU 竞态；且 SELECT → UPDATE/INSERT 三步不在同一事务内。

- [ ] 在 `inv_inventory_transaction (source_type, source_id)` 上加唯一索引
- [ ] 将幂等判定改为 `INSERT IGNORE` + 检查 `affectedRows`
- [ ] 将整个 Handler 逻辑包在 `transaction()` 内
- [ ] 补充并发用例测试

### 2. "伪 Outbox"事务包裹修复

**问题**：`SampleOrderApplicationService.createOrder()` 的聚合写入与事件 Outbox 写入未在统一事务内。

- [ ] 在 `createOrder`、`submitOrder`、`convertToSalesOrder` 方法中用 `transaction()` 包裹 `repo.save()` + `persistEvents()`

### 3. 核心流程端到端测试（T502）

- [ ] 覆盖印前/生产/打样核心链路，重点包含完工入库修复路径与打样转单路径
- [ ] 验证 FinishOrderApprovedEvent 完整链路（route → event bus → inventory handler → DB）

---

## P1 — 高优先级

### 4. API route 层覆盖率提升

**现状**：3 个 route 文件无专门单测，估算覆盖率 35-45%

- [ ] 为 `production-inbound/route.ts` 补充单元测试
- [ ] 为 `sample/orders/status/route.ts` 补充单元测试
- [ ] 为 `SampleOrderApplicationService.createSalesOrderFromSample()` 补充单测
- [ ] 目标：API 层覆盖率达 70%

### 5. Token 黑名单持久化

**现状**：`token-blacklist.ts` 已使用 `CacheManager` 抽象层，配置 `REDIS_URL` 即可自动用 `RedisCacheManager`

- [ ] 生产环境设置 `REDIS_URL` 环境变量
- [ ] 验证 Redis 故障时仍可降级到 `InMemoryCacheManager`

### 6. 限流 Redis 化

**问题**：`src/lib/rate-limit.ts:83` 当 Redis 不可用时降级到内存 `Map`，多实例部署失效

- [ ] 生产环境设置 `REDIS_URL`
- [ ] 考虑是否禁止内存降级（根据部署策略决定）

### 7. 销售订单号并发安全

**问题**：`createSalesOrderFromSample()` 的订单号生成存在并发冲突

- [ ] 在 `sal_order.order_no` 上加 `UNIQUE INDEX`
- [ ] 应用层捕获唯一约束冲突并重试 2-3 次

---

## P2 — 中优先级

### 8. 测试加固
- [ ] 修复 `tests/unit/mrp-engine.test.ts` 中 `vi.mock()` 不在顶层
- [ ] 持续提升覆盖率（当前 1262 测试，目标 80%+）
- [ ] 添加 Demo 模式专用测试

### 9. 降低 ESLint 警告
- [ ] 清理 `no-explicit-any` 警告（5348 个，逐步）
- [ ] 修复 `i18n/no-chinese-hardcode` 6 处（`src/lib/soft-delete.ts`）
- [ ] 清理 `no-unused-vars` 警告

### 10. Schema 逐步对齐（97 张缺失表）
- [ ] 为核心业务表（sys_*, crm_*, qc_*, fin_*）添加 Drizzle 映射
- [ ] 逐步为仓库辅助表（inv_*）添加映射
- [ ] 将 raw SQL 查询迁移到 Drizzle ORM

### 11. 模块文档更新（T503）
- [ ] 更新印前/生产/打样三模块的领域模型、API、状态机文档

---

## P3 — 长期

### 12. 设备/HR 模块迁移 DDD
- [ ] 评估工作量（约 4-6 天）
- [ ] 创建领域实体、仓储接口
- [ ] 从胖路由迁移到应用服务层

### 13. 社区建设
- [ ] 创建 `SECURITY.md` / `CODE_OF_CONDUCT.md`
- [ ] 完善 Issue/PR 模板

### 14. 性能与稳定性
- [ ] 建立性能基准测试
- [ ] 完善错误处理和审计日志

### 15. BaseRepository 抽象增强
- [ ] 将 `buildWhereClause` 改为 abstract，强制子类声明过滤语义
- [ ] 抽取 `DbExecutor` 接口让基类依赖抽象而非 `@/lib/db`
- [ ] 在基类顶部文档中显式声明"硬编码假设清单"

---

## 当前阻塞项
- [ ] **FinishOrderInventoryHandler TOCTOU 竞态修复** — 阻断发布
- [ ] **"伪 Outbox"事务包裹修复** — 阻断发布
- [ ] **T502 E2E 测试补全** — 阻断发布
- [ ] **API route 层覆盖率达 70%** — 建议阻断