// 网版生命周期历史的唯一实现位于 /api/screen-plates/history（见 ../../screen-plates/history/route.ts）。
// 本文件为兼容别名，使 /api/prepress/screen_plate 成为 /api/screen-plates 的完整镜像。
export { GET, POST } from '@/app/api/screen-plates/history/route';
