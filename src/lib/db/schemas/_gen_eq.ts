// AUTO-GENERATED by scripts/_audit/gen_misc.cjs (2026-08-26)
// 域: eq ｜ 来源: live information_schema ｜ 建模 3 张核心缺失表。
// 导出名冲突自动规避（追加 Gen 后缀）；跨域 FK 注释保留；内部/已建模 FK 实写。
import { mysqlTable, int, bigint, smallint, mediumint, tinyint, varchar, char, decimal, float, double, text, date, datetime, timestamp, time, json, mysqlEnum, primaryKey, index, uniqueIndex, foreignKey } from 'drizzle-orm/mysql-core';
import { sql } from 'drizzle-orm';

export const eqEquipment = mysqlTable('eq_equipment', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  equipmentCode: varchar('equipment_code', { length: 50 }).notNull(),
  equipmentName: varchar('equipment_name', { length: 100 }).notNull(),
  equipmentType: varchar('equipment_type', { length: 50 }).notNull().default('other'),
  model: varchar('model', { length: 100 }),
  manufacturer: varchar('manufacturer', { length: 100 }),
  workshop: varchar('workshop', { length: 50 }),
  location: varchar('location', { length: 100 }),
  purchaseDate: date('purchase_date'),
  installDate: date('install_date'),
  purchasePrice: decimal('purchase_price', { precision: 12, scale: 2 }).default('0.00'),
  expectedLifeYears: int('expected_life_years').default(10),
  status: tinyint('status').notNull().default(1),
  cumulativeRunHours: decimal('cumulative_run_hours', { precision: 10, scale: 2 }).default('0.00'),
  cumulativePrintCount: bigint('cumulative_print_count', { mode: 'number' }).default(0),
  lastMaintenanceDate: date('last_maintenance_date'),
  nextMaintenanceDate: date('next_maintenance_date'),
  remark: text('remark'),
  deleted: tinyint('deleted').notNull().default(0),
  createTime: datetime('create_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
}, (t) => ({
  idxEquipmentNextMaintenance: index('idx_equipment_next_maintenance').on(t.nextMaintenanceDate),
  idxEquipmentStatus: index('idx_equipment_status').on(t.status),
  idxEquipmentType: index('idx_equipment_type').on(t.equipmentType),
  idxEquipmentWorkshop: index('idx_equipment_workshop').on(t.workshop),
  pk: primaryKey({ columns: [t.id] }),
  ukEquipmentCode: uniqueIndex('uk_equipment_code').on(t.equipmentCode),
}));

export const eqMaintenancePlan = mysqlTable('eq_maintenance_plan', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  planNo: varchar('plan_no', { length: 50 }).notNull(),
  equipmentId: bigint('equipment_id', { mode: 'number', unsigned: true }).notNull(),
  planName: varchar('plan_name', { length: 100 }).notNull(),
  maintenanceType: varchar('maintenance_type', { length: 20 }).notNull().default('routine'),
  cycleType: varchar('cycle_type', { length: 20 }).notNull().default('monthly'),
  cycleDays: int('cycle_days').default(30),
  leadDays: int('lead_days').default(7),
  estimatedHours: decimal('estimated_hours', { precision: 5, scale: 2 }).default('4.00'),
  estimatedCost: decimal('estimated_cost', { precision: 10, scale: 2 }).default('0.00'),
  checklist: json('checklist'),
  status: tinyint('status').notNull().default(1),
  lastExecutedDate: date('last_executed_date'),
  nextExecuteDate: date('next_execute_date'),
  remark: text('remark'),
  deleted: tinyint('deleted').notNull().default(0),
  createTime: datetime('create_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
}, (t) => ({
  idxPlanEquipment: index('idx_plan_equipment').on(t.equipmentId),
  idxPlanNextExecute: index('idx_plan_next_execute').on(t.nextExecuteDate),
  idxPlanStatus: index('idx_plan_status').on(t.status),
  pk: primaryKey({ columns: [t.id] }),
  ukPlanNo: uniqueIndex('uk_plan_no').on(t.planNo),
  fkPlanEquipment: foreignKey({ columns: [t.equipmentId], foreignColumns: [eqEquipment.id] }),
}));

export const eqMaintenanceRecord = mysqlTable('eq_maintenance_record', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  recordNo: varchar('record_no', { length: 50 }).notNull(),
  equipmentId: bigint('equipment_id', { mode: 'number', unsigned: true }).notNull(),
  planId: bigint('plan_id', { mode: 'number', unsigned: true }),
  maintenanceType: varchar('maintenance_type', { length: 20 }).notNull().default('routine'),
  maintenanceDate: date('maintenance_date').notNull(),
  startTime: datetime('start_time'),
  endTime: datetime('end_time'),
  actualHours: decimal('actual_hours', { precision: 5, scale: 2 }).default('0.00'),
  actualCost: decimal('actual_cost', { precision: 10, scale: 2 }).default('0.00'),
  technicianName: varchar('technician_name', { length: 50 }),
  runHoursBefore: decimal('run_hours_before', { precision: 10, scale: 2 }),
  runHoursAfter: decimal('run_hours_after', { precision: 10, scale: 2 }),
  description: text('description'),
  partsReplaced: json('parts_replaced'),
  result: varchar('result', { length: 20 }).notNull().default('completed'),
  status: tinyint('status').notNull().default(1),
  remark: text('remark'),
  deleted: tinyint('deleted').notNull().default(0),
  createTime: datetime('create_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
}, (t) => ({
  idxRecordDate: index('idx_record_date').on(t.maintenanceDate),
  idxRecordEquipment: index('idx_record_equipment').on(t.equipmentId),
  idxRecordPlan: index('idx_record_plan').on(t.planId),
  idxRecordStatus: index('idx_record_status').on(t.status),
  pk: primaryKey({ columns: [t.id] }),
  ukRecordNo: uniqueIndex('uk_record_no').on(t.recordNo),
  fkRecordEquipment: foreignKey({ columns: [t.equipmentId], foreignColumns: [eqEquipment.id] }),
  fkRecordPlan: foreignKey({ columns: [t.planId], foreignColumns: [eqMaintenancePlan.id] }),
}));

