// AUTO-GENERATED by scripts/_audit/gen_misc.cjs (2026-08-26)
// 域: ink ｜ 来源: live information_schema ｜ 建模 5 张核心缺失表。
// 导出名冲突自动规避（追加 Gen 后缀）；跨域 FK 注释保留；内部/已建模 FK 实写。
import { mysqlTable, int, bigint, smallint, mediumint, tinyint, varchar, char, decimal, float, double, text, date, datetime, timestamp, time, json, mysqlEnum, primaryKey, index, uniqueIndex, foreignKey } from 'drizzle-orm/mysql-core';
import { sql } from 'drizzle-orm';

export const inkMixedBatch = mysqlTable('ink_mixed_batch', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  batchNo: varchar('batch_no', { length: 50 }).notNull(),
  formulaNo: varchar('formula_no', { length: 50 }).default(''),
  formulaName: varchar('formula_name', { length: 200 }).default(''),
  totalQty: decimal('total_qty', { precision: 12, scale: 2 }).default('0.00'),
  unit: varchar('unit', { length: 20 }).default(''),
  mixedDate: date('mixed_date'),
  expireDate: date('expire_date'),
  operatorId: bigint('operator_id', { mode: 'number', unsigned: true }),
  operatorName: varchar('operator_name', { length: 100 }).default(''),
  status: tinyint('status').default(1),
  remark: text('remark'),
  deleted: tinyint('deleted').default(0),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
}, (t) => ({
  pk: primaryKey({ columns: [t.id] }),
  ukBatchNo: uniqueIndex('uk_batch_no').on(t.batchNo),
}));

export const inkMixedBatchDetail = mysqlTable('ink_mixed_batch_detail', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  mixedBatchId: bigint('mixed_batch_id', { mode: 'number', unsigned: true }).notNull(),
  sourceBatchNo: varchar('source_batch_no', { length: 50 }).default(''),
  sourceLabelNo: varchar('source_label_no', { length: 50 }).default(''),
  materialId: bigint('material_id', { mode: 'number', unsigned: true }),
  materialName: varchar('material_name', { length: 200 }).default(''),
  usedQty: decimal('used_qty', { precision: 12, scale: 2 }).default('0.00'),
  unit: varchar('unit', { length: 20 }).default(''),
}, (t) => ({
  pk: primaryKey({ columns: [t.id] }),
}));

export const inkMixedRecord = mysqlTable('ink_mixed_record', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  recordNo: varchar('record_no', { length: 50 }).notNull(),
  baseInkId: bigint('base_ink_id', { mode: 'number', unsigned: true }).notNull(),
  baseInkCode: varchar('base_ink_code', { length: 50 }),
  baseInkName: varchar('base_ink_name', { length: 200 }),
  mixRatio: varchar('mix_ratio', { length: 100 }),
  colorName: varchar('color_name', { length: 100 }),
  colorCode: varchar('color_code', { length: 50 }),
  companyId: bigint('company_id', { mode: 'number', unsigned: true }),
  companyName: varchar('company_name', { length: 200 }),
  mixTime: datetime('mix_time').notNull(),
  operatorId: bigint('operator_id', { mode: 'number', unsigned: true }),
  operatorName: varchar('operator_name', { length: 50 }),
  quantity: decimal('quantity', { precision: 18, scale: 4 }),
  unit: varchar('unit', { length: 20 }),
  warehouseId: bigint('warehouse_id', { mode: 'number', unsigned: true }),
  locationId: bigint('location_id', { mode: 'number', unsigned: true }),
  status: tinyint('status').default(1),
  expireTime: datetime('expire_time'),
  remark: text('remark'),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').default(0),
}, (t) => ({
  idxBaseInk: index('idx_base_ink').on(t.baseInkId),
  idxCompany: index('idx_company').on(t.companyId),
  idxStatus: index('idx_status').on(t.status),
  pk: primaryKey({ columns: [t.id] }),
  ukRecordNo: uniqueIndex('uk_record_no').on(t.recordNo),
}));

export const inkOpeningRecord = mysqlTable('ink_opening_record', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  recordNo: varchar('record_no', { length: 50 }).notNull(),
  materialId: bigint('material_id', { mode: 'number', unsigned: true }).notNull(),
  materialCode: varchar('material_code', { length: 50 }),
  materialName: varchar('material_name', { length: 200 }),
  batchNo: varchar('batch_no', { length: 50 }),
  labelId: bigint('label_id', { mode: 'number', unsigned: true }),
  inkType: varchar('ink_type', { length: 20 }),
  openTime: datetime('open_time').notNull(),
  expireHours: int('expire_hours').notNull(),
  expireTime: datetime('expire_time'),
  remainingQty: decimal('remaining_qty', { precision: 18, scale: 4 }),
  unit: varchar('unit', { length: 20 }),
  status: tinyint('status').default(1),
  operatorId: bigint('operator_id', { mode: 'number', unsigned: true }),
  operatorName: varchar('operator_name', { length: 50 }),
  remark: text('remark'),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').default(0),
}, (t) => ({
  idxBatch: index('idx_batch').on(t.batchNo),
  idxExpireTime: index('idx_expire_time').on(t.expireTime),
  idxMaterial: index('idx_material').on(t.materialId),
  idxStatus: index('idx_status').on(t.status),
  pk: primaryKey({ columns: [t.id] }),
  ukRecordNo: uniqueIndex('uk_record_no').on(t.recordNo),
}));

export const inkUsage = mysqlTable('ink_usage', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  usageNo: varchar('usage_no', { length: 50 }).notNull(),
  usageType: varchar('usage_type', { length: 20 }).notNull(),
  batchNo: varchar('batch_no', { length: 50 }),
  qrCode: varchar('qr_code', { length: 100 }),
  workorderId: bigint('workorder_id', { mode: 'number', unsigned: true }),
  workorderNo: varchar('workorder_no', { length: 50 }),
  formulaId: bigint('formula_id', { mode: 'number', unsigned: true }),
  formulaNo: varchar('formula_no', { length: 50 }),
  colorName: varchar('color_name', { length: 100 }),
  weight: decimal('weight', { precision: 10, scale: 3 }).notNull(),
  unit: varchar('unit', { length: 10 }).default('kg'),
  operatorId: bigint('operator_id', { mode: 'number', unsigned: true }),
  operatorName: varchar('operator_name', { length: 50 }),
  machineId: bigint('machine_id', { mode: 'number', unsigned: true }),
  machineName: varchar('machine_name', { length: 100 }),
  locationId: bigint('location_id', { mode: 'number', unsigned: true }),
  locationName: varchar('location_name', { length: 100 }),
  status: tinyint('status').default(1),
  remark: text('remark'),
  usageTime: datetime('usage_time').default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').default(0),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  inkId: bigint('ink_id', { mode: 'number', unsigned: true }),
  screenPlateId: bigint('screen_plate_id', { mode: 'number', unsigned: true }),
  usageDate: datetime('usage_date'),
  usageQty: decimal('usage_qty', { precision: 10, scale: 3 }),
  inkCode: varchar('ink_code', { length: 50 }),
  inkName: varchar('ink_name', { length: 100 }),
}, (t) => ({
  idxBatchNo: index('idx_batch_no').on(t.batchNo),
  idxUsageTime: index('idx_usage_time').on(t.usageTime),
  idxUsageType: index('idx_usage_type').on(t.usageType),
  idxWorkorder: index('idx_workorder').on(t.workorderNo),
  pk: primaryKey({ columns: [t.id] }),
  ukUsageNo: uniqueIndex('uk_usage_no').on(t.usageNo),
}));

