// AUTO-GENERATED by scripts/_audit/gen_misc.cjs (2026-08-26)
// 域: material ｜ 来源: live information_schema ｜ 建模 5 张核心缺失表。
// 导出名冲突自动规避（追加 Gen 后缀）；跨域 FK 注释保留；内部/已建模 FK 实写。
import { mysqlTable, int, bigint, smallint, mediumint, tinyint, varchar, char, decimal, float, double, text, date, datetime, timestamp, time, json, mysqlEnum, primaryKey, index, uniqueIndex, foreignKey } from 'drizzle-orm/mysql-core';
import { sql } from 'drizzle-orm';
import { invMaterial, invWarehouse } from './warehouse';
import { prdWorkOrder } from './production';

export const materialBatchCosts = mysqlTable('material_batch_costs', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  qrCode: varchar('qr_code', { length: 50 }).notNull(),
  materialId: bigint('material_id', { mode: 'number', unsigned: true }).notNull(),
  materialCode: varchar('material_code', { length: 50 }),
  materialName: varchar('material_name', { length: 100 }),
  batchNo: varchar('batch_no', { length: 50 }),
  quantity: decimal('quantity', { precision: 18, scale: 4 }).notNull(),
  unitCost: decimal('unit_cost', { precision: 18, scale: 4 }).notNull(),
  totalCost: decimal('total_cost', { precision: 18, scale: 4 }).notNull(),
  usedQuantity: decimal('used_quantity', { precision: 18, scale: 4 }).default('0.0000'),
  remainingQuantity: decimal('remaining_quantity', { precision: 18, scale: 4 }).default('0.0000'),
  splitFlag: tinyint('split_flag').default(1),
  warehouseId: bigint('warehouse_id', { mode: 'number', unsigned: true }),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').notNull().default(0),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
}, (t) => ({
  idxBatch: index('idx_batch').on(t.batchNo),
  idxMaterial: index('idx_material').on(t.materialId),
  idxWarehouse: index('idx_warehouse').on(t.warehouseId),
  pk: primaryKey({ columns: [t.id] }),
  ukQrCode: uniqueIndex('uk_qr_code').on(t.qrCode),
  fkMatBatchCostMaterial: foreignKey({ columns: [t.materialId], foreignColumns: [invMaterial.id] }),
  fkMatBatchCostWarehouse: foreignKey({ columns: [t.warehouseId], foreignColumns: [invWarehouse.id] }),
}));

// SKIP-FK (self-ref): material_requisitions.originalRequisitionId -> material_requisitions.id
export const materialRequisitions = mysqlTable('material_requisitions', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  requisitionNo: varchar('requisition_no', { length: 50 }).notNull(),
  workOrderId: bigint('work_order_id', { mode: 'number', unsigned: true }),
  workOrderNo: varchar('work_order_no', { length: 50 }),
  type: varchar('type', { length: 20 }).notNull(),
  status: tinyint('status').default(0),
  applicantId: bigint('applicant_id', { mode: 'number', unsigned: true }),
  applicantName: varchar('applicant_name', { length: 50 }),
  approverId: bigint('approver_id', { mode: 'number', unsigned: true }),
  approverName: varchar('approver_name', { length: 50 }),
  approveTime: datetime('approve_time'),
  totalQuantity: decimal('total_quantity', { precision: 18, scale: 4 }).default('0.0000'),
  issuedQuantity: decimal('issued_quantity', { precision: 18, scale: 4 }).default('0.0000'),
  warehouseId: bigint('warehouse_id', { mode: 'number', unsigned: true }),
  originalRequisitionId: bigint('original_requisition_id', { mode: 'number', unsigned: true }),
  reason: varchar('reason', { length: 255 }),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
  remark: text('remark'),
  deleted: tinyint('deleted').notNull().default(0),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
}, (t) => ({
  idxApplicant: index('idx_applicant').on(t.applicantId),
  idxApprover: index('idx_approver').on(t.approverId),
  idxOriginalRequisition: index('idx_original_requisition').on(t.originalRequisitionId),
  idxStatus: index('idx_status').on(t.status),
  idxType: index('idx_type').on(t.type),
  idxWarehouse: index('idx_warehouse').on(t.warehouseId),
  idxWorkOrder: index('idx_work_order').on(t.workOrderId),
  pk: primaryKey({ columns: [t.id] }),
  ukRequisitionNo: uniqueIndex('uk_requisition_no').on(t.requisitionNo),
  fkMatReqWarehouse: foreignKey({ columns: [t.warehouseId], foreignColumns: [invWarehouse.id] }),
  fkMatReqWorkOrder: foreignKey({ columns: [t.workOrderId], foreignColumns: [prdWorkOrder.id] }),
}));

export const materialRequisitionItems = mysqlTable('material_requisition_items', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  requisitionId: bigint('requisition_id', { mode: 'number', unsigned: true }).notNull(),
  materialId: bigint('material_id', { mode: 'number', unsigned: true }).notNull(),
  materialCode: varchar('material_code', { length: 50 }),
  materialName: varchar('material_name', { length: 100 }),
  plannedQuantity: decimal('planned_quantity', { precision: 18, scale: 4 }).notNull(),
  actualQuantity: decimal('actual_quantity', { precision: 18, scale: 4 }).default('0.0000'),
  issuedQuantity: decimal('issued_quantity', { precision: 18, scale: 4 }).default('0.0000'),
  unit: varchar('unit', { length: 20 }),
  qrCode: varchar('qr_code', { length: 50 }),
  batchNo: varchar('batch_no', { length: 50 }),
  warehouseLocation: varchar('warehouse_location', { length: 50 }),
  fifoRecommended: tinyint('fifo_recommended').default(0),
  splitFlag: tinyint('split_flag').default(1),
  unitCost: decimal('unit_cost', { precision: 18, scale: 4 }),
  totalCost: decimal('total_cost', { precision: 18, scale: 4 }),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').notNull().default(0),
}, (t) => ({
  idxMaterial: index('idx_material').on(t.materialId),
  idxQrCode: index('idx_qr_code').on(t.qrCode),
  idxRequisition: index('idx_requisition').on(t.requisitionId),
  pk: primaryKey({ columns: [t.id] }),
  fkMatReqItemMaterial: foreignKey({ columns: [t.materialId], foreignColumns: [invMaterial.id] }),
  fkReqItemsRequisition: foreignKey({ columns: [t.requisitionId], foreignColumns: [materialRequisitions.id] }),
}));

export const materialReturns = mysqlTable('material_returns', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  returnNo: varchar('return_no', { length: 50 }).notNull(),
  workOrderId: bigint('work_order_id', { mode: 'number', unsigned: true }),
  requisitionId: bigint('requisition_id', { mode: 'number', unsigned: true }),
  status: tinyint('status').default(0),
  applicantId: bigint('applicant_id', { mode: 'number', unsigned: true }),
  applicantName: varchar('applicant_name', { length: 50 }),
  confirmId: bigint('confirm_id', { mode: 'number', unsigned: true }),
  confirmName: varchar('confirm_name', { length: 50 }),
  confirmTime: datetime('confirm_time'),
  totalQuantity: decimal('total_quantity', { precision: 18, scale: 4 }).default('0.0000'),
  warehouseId: bigint('warehouse_id', { mode: 'number', unsigned: true }),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
  remark: text('remark'),
  deleted: tinyint('deleted').notNull().default(0),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
}, (t) => ({
  idxApplicant: index('idx_applicant').on(t.applicantId),
  idxConfirm: index('idx_confirm').on(t.confirmId),
  idxRequisition: index('idx_requisition').on(t.requisitionId),
  idxStatus: index('idx_status').on(t.status),
  idxWarehouse: index('idx_warehouse').on(t.warehouseId),
  idxWorkOrder: index('idx_work_order').on(t.workOrderId),
  pk: primaryKey({ columns: [t.id] }),
  ukReturnNo: uniqueIndex('uk_return_no').on(t.returnNo),
  fkMatRetRequisition: foreignKey({ columns: [t.requisitionId], foreignColumns: [materialRequisitions.id] }),
  fkMatRetWarehouse: foreignKey({ columns: [t.warehouseId], foreignColumns: [invWarehouse.id] }),
  fkMatRetWorkOrder: foreignKey({ columns: [t.workOrderId], foreignColumns: [prdWorkOrder.id] }),
}));

export const materialReturnItems = mysqlTable('material_return_items', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  returnId: bigint('return_id', { mode: 'number', unsigned: true }).notNull(),
  materialId: bigint('material_id', { mode: 'number', unsigned: true }).notNull(),
  materialCode: varchar('material_code', { length: 50 }),
  materialName: varchar('material_name', { length: 100 }),
  quantity: decimal('quantity', { precision: 18, scale: 4 }).notNull(),
  unit: varchar('unit', { length: 20 }),
  qrCode: varchar('qr_code', { length: 50 }),
  batchNo: varchar('batch_no', { length: 50 }),
  reason: varchar('reason', { length: 255 }),
  unitCost: decimal('unit_cost', { precision: 18, scale: 4 }),
  totalCost: decimal('total_cost', { precision: 18, scale: 4 }),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').notNull().default(0),
}, (t) => ({
  idxMaterial: index('idx_material').on(t.materialId),
  idxReturn: index('idx_return').on(t.returnId),
  pk: primaryKey({ columns: [t.id] }),
  fkMatRetItemMaterial: foreignKey({ columns: [t.materialId], foreignColumns: [invMaterial.id] }),
  fkRetItemsReturn: foreignKey({ columns: [t.returnId], foreignColumns: [materialReturns.id] }),
}));

