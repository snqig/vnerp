// AUTO-GENERATED by scripts/_audit/gen_misc.cjs (2026-08-26)
// 域: qc ｜ 来源: live information_schema ｜ 建模 5 张核心缺失表。
// 导出名冲突自动规避（追加 Gen 后缀）；跨域 FK 注释保留；内部/已建模 FK 实写。
import { mysqlTable, int, bigint, smallint, mediumint, tinyint, varchar, char, decimal, float, double, text, date, datetime, timestamp, time, json, mysqlEnum, primaryKey, index, uniqueIndex, foreignKey } from 'drizzle-orm/mysql-core';
import { sql } from 'drizzle-orm';
import { invMaterial } from './warehouse';
import { sysUser } from './system';

export const qcAqlSamplingPlan = mysqlTable('qc_aql_sampling_plan', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  lotSizeMin: int('lot_size_min').notNull(),
  lotSizeMax: int('lot_size_max').notNull(),
  sampleSizeCode: varchar('sample_size_code', { length: 5 }).notNull(),
  sampleSize: int('sample_size').notNull(),
  aqlLevel: varchar('aql_level', { length: 10 }).notNull(),
  acceptQty: int('accept_qty').notNull(),
  rejectQty: int('reject_qty').notNull(),
  inspectionLevel: varchar('inspection_level', { length: 10 }).notNull().default('II'),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
}, (t) => ({
  idxAqlLevel: index('idx_aql_level').on(t.aqlLevel),
  idxLotSize: index('idx_lot_size').on(t.lotSizeMin, t.lotSizeMax),
  pk: primaryKey({ columns: [t.id] }),
  ukPlan: uniqueIndex('uk_plan').on(t.lotSizeMin, t.lotSizeMax, t.aqlLevel, t.inspectionLevel),
}));

export const qcIncomingInspection = mysqlTable('qc_incoming_inspection', {
  id: int('id').autoincrement().notNull(),
  inspectionNo: varchar('inspection_no', { length: 50 }).notNull(),
  inspectionDate: date('inspection_date').notNull(),
  supplierName: varchar('supplier_name', { length: 100 }).notNull(),
  materialCode: varchar('material_code', { length: 50 }),
  materialName: varchar('material_name', { length: 100 }).notNull(),
  specification: varchar('specification', { length: 100 }).notNull(),
  batchNo: varchar('batch_no', { length: 50 }).notNull(),
  quantity: decimal('quantity', { precision: 10, scale: 2 }).notNull(),
  unit: varchar('unit', { length: 20 }).notNull(),
  inspectionType: varchar('inspection_type', { length: 20 }).notNull(),
  inspectionResult: varchar('inspection_result', { length: 20 }).notNull(),
  inspectorName: varchar('inspector_name', { length: 50 }).notNull(),
  remark: text('remark'),
  createTime: datetime('create_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').notNull().default(0),
}, (t) => ({
  idxBatchNo: index('idx_batch_no').on(t.batchNo),
  idxInspectionDate: index('idx_inspection_date').on(t.inspectionDate),
  idxInspectionResult: index('idx_inspection_result').on(t.inspectionResult),
  idxMaterialName: index('idx_material_name').on(t.materialName),
  idxSupplierName: index('idx_supplier_name').on(t.supplierName),
  pk: primaryKey({ columns: [t.id] }),
  ukInspectionNo: uniqueIndex('uk_inspection_no').on(t.inspectionNo),
}));

export const qcInspection = mysqlTable('qc_inspection', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  inspectionNo: varchar('inspection_no', { length: 50 }).notNull(),
  inspectionType: tinyint('inspection_type'),
  sourceType: varchar('source_type', { length: 50 }),
  sourceNo: varchar('source_no', { length: 50 }),
  materialId: bigint('material_id', { mode: 'number', unsigned: true }),
  batchNo: varchar('batch_no', { length: 50 }),
  inspectionQty: decimal('inspection_qty', { precision: 18, scale: 4 }).default('0.0000'),
  qualifiedQty: decimal('qualified_qty', { precision: 18, scale: 4 }).default('0.0000'),
  unqualifiedQty: decimal('unqualified_qty', { precision: 18, scale: 4 }).default('0.0000'),
  inspectionResult: tinyint('inspection_result'),
  inspector: varchar('inspector', { length: 50 }),
  inspectionDate: date('inspection_date'),
  remark: text('remark'),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').notNull().default(0),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
}, (t) => ({
  idxMaterial: index('idx_material').on(t.materialId),
  idxSource: index('idx_source').on(t.sourceNo),
  idxType: index('idx_type').on(t.inspectionType),
  pk: primaryKey({ columns: [t.id] }),
  ukInspectionNo: uniqueIndex('uk_inspection_no').on(t.inspectionNo),
  fkQcInspectionMaterial: foreignKey({ columns: [t.materialId], foreignColumns: [invMaterial.id] }),
}));

export const qcUnqualified = mysqlTable('qc_unqualified', {
  id: bigint('id', { mode: 'number', unsigned: true }).autoincrement().notNull(),
  unqualifiedNo: varchar('unqualified_no', { length: 50 }).notNull(),
  handleNo: varchar('handle_no', { length: 50 }),
  inspectionId: bigint('inspection_id', { mode: 'number', unsigned: true }),
  sourceType: varchar('source_type', { length: 50 }),
  sourceNo: varchar('source_no', { length: 50 }),
  materialId: bigint('material_id', { mode: 'number', unsigned: true }),
  materialCode: varchar('material_code', { length: 50 }),
  materialName: varchar('material_name', { length: 100 }),
  quantity: decimal('quantity', { precision: 18, scale: 4 }).default('0.0000'),
  defectType: varchar('defect_type', { length: 50 }),
  defectDesc: text('defect_desc'),
  handleType: tinyint('handle_type'),
  handleStatus: tinyint('handle_status').notNull().default(1),
  responsibleDept: varchar('responsible_dept', { length: 100 }),
  responsiblePerson: varchar('responsible_person', { length: 50 }),
  handleResult: tinyint('handle_result'),
  costAmount: decimal('cost_amount', { precision: 18, scale: 4 }).default('0.0000'),
  handler: varchar('handler', { length: 50 }),
  handleDate: date('handle_date'),
  remark: text('remark'),
  createTime: datetime('create_time').default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').default(sql`CURRENT_TIMESTAMP`),
  createBy: bigint('create_by', { mode: 'number', unsigned: true }),
  updateBy: bigint('update_by', { mode: 'number', unsigned: true }),
  deleted: tinyint('deleted').notNull().default(0),
}, (t) => ({
  idxHandleStatus: index('idx_handle_status').on(t.handleStatus),
  idxInspection: index('idx_inspection').on(t.inspectionId),
  idxMaterial: index('idx_material').on(t.materialId),
  idxMaterialId: index('idx_material_id').on(t.materialId),
  idxSource: index('idx_source').on(t.sourceType, t.sourceNo),
  pk: primaryKey({ columns: [t.id] }),
  ukHandleNo: uniqueIndex('uk_handle_no').on(t.handleNo),
  ukUnqualifiedNo: uniqueIndex('uk_unqualified_no').on(t.unqualifiedNo),
  fkQcUnqualifiedCreateBy: foreignKey({ columns: [t.createBy], foreignColumns: [sysUser.id] }),
  fkQcUnqualifiedMaterial: foreignKey({ columns: [t.materialId], foreignColumns: [invMaterial.id] }),
  fkQcUnqualifiedUpdateBy: foreignKey({ columns: [t.updateBy], foreignColumns: [sysUser.id] }),
}));

export const qcIncomingInspectionItem = mysqlTable('qc_incoming_inspection_item', {
  id: int('id').autoincrement().notNull(),
  inspectionId: int('inspection_id').notNull(),
  inspectionNo: varchar('inspection_no', { length: 50 }).notNull(),
  itemName: varchar('item_name', { length: 100 }).notNull(),
  standard: varchar('standard', { length: 255 }).notNull(),
  actualValue: varchar('actual_value', { length: 255 }),
  result: varchar('result', { length: 20 }).notNull(),
  remark: text('remark'),
  createTime: datetime('create_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  updateTime: datetime('update_time').notNull().default(sql`CURRENT_TIMESTAMP`),
  deleted: tinyint('deleted').notNull().default(0),
}, (t) => ({
  idxInspectionId: index('idx_inspection_id').on(t.inspectionId),
  idxInspectionNo: index('idx_inspection_no').on(t.inspectionNo),
  idxItemName: index('idx_item_name').on(t.itemName),
  idxResult: index('idx_result').on(t.result),
  pk: primaryKey({ columns: [t.id] }),
  fkQcIncomingItemInspection: foreignKey({ columns: [t.inspectionId], foreignColumns: [qcIncomingInspection.id] }),
}));

